{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd apps/api && pip install -r requirements.txt && python -m prisma generate",
        "watchPatterns": [
            "apps/api/**"
        ]
    },
    "deploy": {
        "startCommand": "cd apps/api && python -m prisma generate && python -m prisma db push --accept-data-loss && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 180,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}