name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # ============= BACKEND TESTS =============
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: vmp_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Cache Python dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('apps/api/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: Install dependencies
        working-directory: apps/api
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      
      - name: Generate Prisma Client
        working-directory: apps/api
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/vmp_test
        run: |
          prisma generate
          prisma db push --skip-generate
      
      - name: Run Linting
        working-directory: apps/api
        run: |
          pip install flake8 black
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          black --check .
        continue-on-error: true
      
      - name: Run Tests
        working-directory: apps/api
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/vmp_test
          JWT_SECRET: test-secret-key-for-ci
          SMTP_PASSWORD: ""
        run: |
          pytest tests/ -v --cov=. --cov-report=xml --cov-report=term
      
      - name: Upload Coverage
        uses: codecov/codecov-action@v3
        with:
          file: apps/api/coverage.xml
          flags: backend
          name: backend-coverage
        continue-on-error: true
  
  # ============= FRONTEND TESTS =============
  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/web/package-lock.json
      
      - name: Install dependencies
        working-directory: apps/web
        run: npm ci
      
      - name: Run Linting
        working-directory: apps/web
        run: npm run lint
        continue-on-error: true
      
      - name: Run Type Check
        working-directory: apps/web
        run: npm run type-check
        continue-on-error: true
      
      - name: Build
        working-directory: apps/web
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8000
        run: npm run build
  
  # ============= E2E TESTS =============
  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [backend-tests, frontend-tests]
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: vmp_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Backend Dependencies
        working-directory: apps/api
        run: |
          pip install -r requirements.txt
          prisma generate
          prisma db push --skip-generate
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/vmp_test
      
      - name: Install Frontend Dependencies
        working-directory: apps/web
        run: npm ci
      
      - name: Install Playwright
        working-directory: apps/web
        run: npx playwright install --with-deps chromium
      
      - name: Start Backend
        working-directory: apps/api
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/vmp_test
          JWT_SECRET: test-secret-key
          SMTP_PASSWORD: ""
        run: |
          uvicorn main:app --host 0.0.0.0 --port 8000 &
          sleep 5
      
      - name: Start Frontend
        working-directory: apps/web
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8000
        run: |
          npm run build
          npm run start &
          sleep 10
      
      - name: Run E2E Tests
        working-directory: apps/web
        run: npx playwright test
        continue-on-error: true
      
      - name: Upload Playwright Report
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: playwright-report
          path: apps/web/playwright-report/
          retention-days: 30
  
  # ============= DEPLOY TO STAGING =============
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [backend-tests, frontend-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy Backend to Railway
        run: |
          echo "Backend deployment would happen here"
          echo "Railway CLI or API integration"
        # TODO: Add Railway deployment
      
      - name: Deploy Frontend to Vercel
        run: |
          echo "Frontend deployment would happen here"
          echo "Vercel CLI integration"
        # TODO: Add Vercel deployment
      
      - name: Run Smoke Tests
        run: |
          echo "Smoke tests on staging would run here"
        # TODO: Add smoke tests
